{
    "name": "@ivakit/database",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "db:push": "drizzle-kit push",
        "db:migrate": "drizzle-kit migrate",
        "db:generate": "drizzle-kit generate",
        "db:seed": "bun run src/seed.ts",
        "db:studio": "drizzle-kit studio",
        "build": "tsc",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@ivakit/shared": "workspace:*",
        "@libsql/client": "^0.6.0",
        "drizzle-orm": "^0.30.0"
    },
    "devDependencies": {
        "drizzle-kit": "^0.21.0",
        "typescript": "^5.3.0"
    }
}